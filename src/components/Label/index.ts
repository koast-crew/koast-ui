// 내부 전용입니다. Checkbox / Radio / Switch / ControlGroup 이 라벨을 그릴 때만 씁니다.
// 공개 API 가 아니므로 src/index.ts 에서 export 하지 않습니다.
export { Label as default } from './Label';
export type { LabelProps, LabelType, LabelAs } from './Label.types';
